// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import React, { useMemo } from 'react';
import { SendBox as SimpleSendBox } from "../../../../react-components/src";
import { usePropsFor } from '../ChatComposite/hooks/usePropsFor';
/**
 * @private
 */
export const SendBox = (props) => {
    const { options, styles } = props;
    const sendBoxProps = usePropsFor(SimpleSendBox);
    const sendBoxStyles = useMemo(() => {
        return Object.assign({}, styles);
    }, [styles]);
    const simpleSendBox = useMemo(() => React.createElement(SimpleSendBox, Object.assign({}, sendBoxProps, { autoFocus: options === null || options === void 0 ? void 0 : options.autoFocus, styles: sendBoxStyles })), [sendBoxProps, options, sendBoxStyles]);
    return simpleSendBox;
};
//# sourceMappingURL=SendBox.js.map